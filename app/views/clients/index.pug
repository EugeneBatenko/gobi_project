extends ../layout

block content
  .container
    form(action='/' method='POST')
      a(href="/add_client") New user
      table.table
        thead.table-dark
          tr
            th ID
            th Name
            th Email
            th Gender
            th Status
            th Action
        tbody
          include _show
